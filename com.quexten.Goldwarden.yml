id: com.quexten.Goldwarden
runtime: org.gnome.Platform
runtime-version: '45'
sdk: org.gnome.Sdk
command: goldwarden_ui_main.py
finish-args:
  # Allow network access for sync
  - --share=network

  # GUI
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri

  # Lock on screensave
  - --talk-name=org.gnome.ScreenSaver
  - --talk-name=org.freedesktop.ScreenSaver

  # pinentry & approval
  - --talk-name=org.gnome.keyring.SystemPrompter
  # biometric / user password auth
  - --system-talk-name=org.freedesktop.PolicyKit1
modules:
  - name: blueprint-compiler
    buildsystem: meson
    cleanup: ['*']
    sources:
      - type: git
        url: https://gitlab.gnome.org/jwestman/blueprint-compiler
        tag: v0.12.0
  - ./python3-requirements.json
  - name: goldwarden-python-ui
    buildsystem: simple
    build-commands:
      - mkdir -p /app/bin
      - cp -R ./gui/* /app/bin
      - chmod +x /app/bin/goldwarden_ui_main.py
      - install -D ./gui/com.quexten.Goldwarden.desktop /app/share/applications/com.quexten.Goldwarden.desktop
      - install -D ./gui/goldwarden.svg /app/share/icons/hicolor/scalable/apps/com.quexten.Goldwarden.svg
      - install -Dm644 ./com.quexten.Goldwarden.metainfo.xml -t /app/share/metainfo/
      - blueprint-compiler batch-compile /app/bin/src/gui/.templates/ /app/bin/src/gui/
        /app/bin/src/gui/*.blp
    sources:
      - type: archive
        url: https://github.com/quexten/goldwarden/archive/refs/tags/v0.3.5.tar.gz
        sha256: 108d0f1053fe2cbcade2f37ccc19c1c677f7a286ea6c11dba1c7a5970e432a72
        x-checker-data:
          type: json
          url: https://api.github.com/repos/quexten/goldwarden/releases/latest
          version-query: .tag_name
          url-query: '"https://github.com/quexten/goldwarden/archive/refs/tags/" +
            .tag_name + ".tar.gz"'
      - type: file
        path: ./com.quexten.Goldwarden.metainfo.xml
  - name: goldwarden-core-daemon
    buildsystem: simple
    build-commands:
      - install -D goldwarden_linux_* /app/bin/goldwarden
    sources:
      - type: file
        url: https://github.com/quexten/goldwarden/releases/download/v0.3.5/goldwarden_linux_x86_64
        sha256: 570c7bdabc8693837e900c86cb89f3af130113afa22ab41b89cef04064004707
        only-arches: [x86_64]
        x-checker-data:
          type: json
          url: https://api.github.com/repos/quexten/goldwarden/releases/latest
          version-query: .tag_name
          url-query: .assets[] | select(.name=="goldwarden_linux_x86_64") | .browser_download_url
      - type: file
        url: https://github.com/quexten/goldwarden/releases/download/v0.3.5/goldwarden_linux_arm64
        sha256: 2d0fcf7e5f45304cfc0513efb0b082be990d339bda6d605d4f3aa811c1d5c6cb
        only-arches: [aarch64]
        x-checker-data:
          type: json
          url: https://api.github.com/repos/quexten/goldwarden/releases/latest
          version-query: .tag_name
          url-query: .assets[] | select(.name=="goldwarden_linux_arm64") | .browser_download_url
